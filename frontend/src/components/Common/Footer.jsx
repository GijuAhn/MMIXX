import styled from 'styled-components'
import { Wrapper } from "."

const Footer = () => {
  return (
    <StyleWrapper>
      ""
    </StyleWrapper>
  )
}

const StyleWrapper = styled(Wrapper)`
  height: 200px;
  padding: 0;
`

export default Footer